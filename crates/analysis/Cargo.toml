[package]
name = "dasmeter-analysis"
description = "Meter analysis: audio frames in, Meter readings out. No platform, GPU or audio-device code."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
# Pinned exactly (ADR 0005): expected values only move when we choose to update.
ebur128 = "=0.1.10"
realfft = "3.5"

[[bench]]
name = "loudness"
harness = false

[[bench]]
name = "spectrum"
harness = false
