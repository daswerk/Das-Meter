# Das-Meter

## Agent skills

### Issue tracker

Issues and specs live in GitHub Issues for daswerk/Das-Meter. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` plus `docs/adr/` at the repo root. See `docs/agents/domain.md`.

## Licence

Das-Meter is dual-licensed **MIT OR Apache-2.0** (`LICENSE-MIT`, `LICENSE-APACHE`), copyright Das Werk, for the whole workspace. Contributions are accepted under the same terms (inbound = outbound); no CLA or DCO.

- **Never copy code from GPL-licensed projects** (e.g. MiniMetersServer, BlackHole). Read them for ideas only; write our own implementation.
- Only add dependencies whose licences are compatible with MIT OR Apache-2.0.
